#!/usr/bin/env python
from json import loads
from sys import stdin
from numpy import array
from numpystring import to_string


def main():
    for line in stdin:
        print(to_string(array(loads(line))))


if __name__ == "__main__":
    main()
