#!/usr/bin/env bash
set -evxo pipefail
andrew | log | trans $LANGUAGE | log > out.txt
ramah media.png < out.txt >&2
echo >> out.txt
echo See full chapter https://searchyah.app/$(<book.txt)-$(<chap.txt) >> out.txt
fb --media media.png < out.txt
