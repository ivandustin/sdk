#!/usr/bin/env python
from sys import stdin
from numpystring import to_array


def main():
    for line in stdin:
        print(" ".join(map(str, to_array(line).shape)))


if __name__ == "__main__":
    main()
