#!/usr/bin/env python
from sys import argv, stdin
from numpy import array


def main():
    expr = " ".join(argv[1:])
    for line in stdin:
        x = array(line.split()).astype(float)
        y = eval(expr)
        print(" ".join(y.astype(str).tolist()))


if __name__ == "__main__":
    main()
