#!/usr/bin/env python3
from json import loads
from sys import stdin


def main():
    for line in stdin:
        value = loads(line)
        assert isinstance(value, str)
        print(value)


if __name__ == "__main__":
    main()
