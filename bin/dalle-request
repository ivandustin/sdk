#!/usr/bin/env python3
from json import dumps
from sys import stdin


def main():
    data = stdin.read().strip()
    print(dumps({"model": "dall-e-3", "prompt": data}))


if __name__ == "__main__":
    main()
