#!/usr/bin/env python
import json
import sys

dictionary = json.load(sys.stdin)
key = sys.argv[1]
value = dictionary[key]
output = value if isinstance(value, str) else json.dumps(value, indent=4)
print(output)
