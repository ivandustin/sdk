#!/usr/bin/env python3
from json import load, dumps
from sys import stdin, argv

value = load(stdin)
keys = argv[1:]
for key in keys:
    value = value[int(key)] if isinstance(value, list) else value[key]
print(value if isinstance(value, str) else dumps(value, indent=4))
