#!/usr/bin/env python
from sys import stdin
from numpy import stack
from npstr import encode

for line in stdin:
    items = line.strip().split(" ")
    indices = list(map(int, items))
    positions = list(range(len(indices)))
    print(encode(stack([positions, indices])))
