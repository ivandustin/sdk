#!/usr/bin/env python
import sys

def main():
    delimiter = sys.argv[1] if len(sys.argv) > 1 else ' '
    print(delimiter.join(line.strip() for line in sys.stdin))

if __name__ == '__main__':
    main()
