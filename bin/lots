#!/usr/bin/env python
from sys import argv


def main():
    equity = float(argv[1])
    risk = int(argv[2])
    lots = equity / (risk * 0.0001) / 100_000
    print(f"{lots:.2f}")


if __name__ == "__main__":
    main()
