#!/usr/bin/env bash
set -euvxo pipefail
file=$1
objects < $file | log > objects.txt
pick < objects.txt | log > object.txt
describe $(cat object.txt) < $file | log | focus | log > desc.txt
dalle < desc.txt | tee image.txt
