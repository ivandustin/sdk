#!/usr/bin/env python3
from math import sqrt
from sys import argv


def main():
    phi = (1 + sqrt(5)) / 2
    value = float(argv[1])
    print(value)
    while value >= 1:
        value /= phi
        print(f"{value:.2f}")


if __name__ == "__main__":
    main()
